# The production application lives in backend/.
# Copy backend/.env.example to backend/.env and replace placeholders locally.
# Never commit backend/.env or real database, wallet, provider, or operator credentials.
